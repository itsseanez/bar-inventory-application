<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Items</title>
</head>
<body>
    <header>
        <h1><a href="/">Bar Inventory</a></h1>
        <div id="link-group">
            <a href="/items">View Items</a>
            <a href="/categories">View Categories</a>
            <a href="/subcategories">View Sub Categories</a>
        </div>
    </header>
    <main>
        <form action="/items" method="POST">
            <input type="text" name="name" placeholder="Name" required>
            <select id="category" name="category" required>
                <option value="" disabled selected>Category</option>
                <% categories.forEach(category => { %>
                    <option value="<%= category.name %>"><%= category.name %></option>
                <% }) %>
            </select>
            <select id="subcategory" name="subcategory">
                <option value="" disabled selected>Sub Category</option>
                <% subcategories.forEach(subcategory => { %>
                    <option value="<%= subcategory.name %>"><%= subcategory.name %></option>
                <% }) %>
            </select>
            <input type="number" name="quantity" placeholder="Quantity" required min="0">
            <button type="submit">Add Item</button>
        </form>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Quantity</th>
                    <th>Category ID</th>
                    <th>Sub Category ID</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% items.forEach(item => { %>
                    <tr>
                        <td><%= item.id %></td>
                        <td><%= item.name %></td>
                        <td><%= item.quantity %></td>
                        <td><%= item.category_id %></td>
                        <td><%= item.subcategory_id %></td>
                        <td><button class="delete-btn" data-delete="items" data-id="<%= item.id %>"><svg fill="currentColor" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><title>trash-can-outline</title><path d="M9,3V4H4V6H5V19A2,2 0 0,0 7,21H17A2,2 0 0,0 19,19V6H20V4H15V3H9M7,6H17V19H7V6M9,8V17H11V8H9M13,8V17H15V8H13Z"></path></svg></button></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </main>
    <script src="/main.js"></script>
    <link rel="stylesheet" href="/styles.css">
</body>
</html>